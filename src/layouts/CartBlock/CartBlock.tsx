import React from 'react';

import CartList from '../../components/CartList/CartList';

const CartBlock = () => {
  return (
    <div>
      <CartList />
    </div>
  );
};

export default CartBlock;
